## Phases
- [x] Phase 1: "Project & State Machine" → steps 1-3
- [x] Phase 2: "Engine Communication" → steps 4-8
- [x] Phase 3: "Input & Context" → steps 9-12
- [x] Phase 4: "UI Components" → steps 13-17
- [x] Phase 5: "Text Output" → steps 18-20
- [x] Phase 6: "App Integration" → steps 21-23
- [x] Phase 7: "Human Verification" → steps 24-33

## Steps

- [x] [devops] Create `app/` Xcode project: target `OpenVerb` (macOS app, SwiftUI lifecycle, deployment target macOS 13+); test target `OpenVerbTests`; directory tree matching spec: `App/`, `Engine/`, `Input/`, `Context/`, `UI/`, `Output/`, `State/`, `Resources/Sounds/`; no third-party dependencies; frameworks to link: `AVFoundation`, `Carbon` (for CGEvent virtual key codes), `ApplicationServices` (for CGEvent posting); set `LSUIElement = true` in Info.plist (menu bar app, no dock icon); add entitlements file with `com.apple.security.device.audio-input`; add `NSMicrophoneUsageDescription = "OpenVerb needs microphone access to record your voice for transcription"` to Info.plist; define shared constants in `app/OpenVerb/Engine/Constants.swift`: `DEFAULT_SOCKET_PATH = "~/.openverb/engine.sock"` (expand `~` via `FileManager.default.homeDirectoryForCurrentUser`), `DEFAULT_MODEL_DIR = "~/.openverb/models/"`, `CHUNK_BYTES = 4096`; **note**: `client/` (MVP2 CLI prototype) is retained for reference only — NOT part of the `app/` build target; MVP3 rewrites EngineClient/EngineManager/EngineProtocol with Swift concurrency (async/await, @MainActor, ObservableObject)

- [x] [architect] Write `app/OpenVerb/State/AppState.swift`: `@MainActor class AppState: ObservableObject` with `@Published var state: State` enum `{ idle, preparing, recording, inferring, error(String) }`; `@Published var targetApp: NSRunningApplication?` — captured at the moment ⌥Space was first pressed on IDLE→PREPARING transition (do NOT re-query frontmostApplication at injection time — RecordingWindow may be foreground by then); `@Published var preparingSubtitle: String?` — set to `"Preparing..."` if `session.ready` not received within 500ms of entering PREPARING, set to `"Reconnecting..."` during abort-and-restart reconnect window (up to ~1.5s); cleared on transition to RECORDING; expose `func transition(to: State)` that enforces valid transitions: IDLE→PREPARING, PREPARING→RECORDING, PREPARING→ERROR, PREPARING→IDLE (cancel during preparing), RECORDING→INFERRING, RECORDING→IDLE (cancel), INFERRING→IDLE (result received), INFERRING→PREPARING (abort+restart — **preserves targetApp**, does NOT clear it), any→ERROR (but ERROR→ERROR is ignored — guard against duplicate error callbacks), **ERROR→PREPARING** (⌥Space during error auto-clear window — clears error immediately and starts new session; do NOT make user wait 5s), ERROR→IDLE; on IDLE→PREPARING: capture `targetApp = NSWorkspace.shared.frontmostApplication`; on INFERRING→PREPARING: keep existing `targetApp` (user is restarting, same target); on ERROR→PREPARING: keep existing `targetApp` or re-capture if nil; on transition to IDLE: clear `targetApp`, clear `preparingSubtitle`; on transition to ERROR: start 5s auto-clear timer → transition to IDLE if no other transition happened; **⌥Space during error**: ⌥Space fires ERROR→PREPARING immediately, cancelling the auto-clear timer

- [x] [tdd-guide] Write `app/OpenVerbTests/AppStateTests.swift`: test valid transitions — IDLE→PREPARING sets targetApp, PREPARING→RECORDING clears preparingSubtitle, PREPARING→IDLE (cancel during preparing) clears targetApp, RECORDING→INFERRING, INFERRING→IDLE clears targetApp, **INFERRING→PREPARING (abort+restart) preserves targetApp** (verify targetApp is same object before and after), RECORDING→IDLE (cancel) clears targetApp; test invalid transitions — IDLE→RECORDING throws/asserts, IDLE→INFERRING throws; test preparingSubtitle set/clear lifecycle; test error state from any state; test ERROR→ERROR ignored (no crash/double-transition); **test ERROR→PREPARING** — transition clears error and captures/keeps targetApp, auto-clear timer is cancelled; test 5s auto-clear — mock timer fires → state becomes IDLE

- [x] [architect] Write `app/OpenVerb/Engine/EngineProtocol.swift`: `ServerMessage` enum with associated values: `.ready`, `.pong`, `.progress(percent: Double)`, `.result(text: String?, command: Command?)`, `.error(code: String, message: String)`, `.warning(code: String, message: String)` — warning does NOT end the session; client logs it and continues streaming; `Command: Decodable` with `action: String` (non-optional; if JSON has `command` key but `action` is missing or empty → treat as no command, use text result instead); decode via `struct TypeProbe: Decodable { let type: String }` → decode raw data with TypeProbe first, then switch on type to decode the specific message; client→server structs: `SessionStart` with `type = "session.start"` and `context: [String: String]`; `Ping` with `type = "ping"`; `Shutdown` with `type = "session.shutdown"`; **no `SessionEnd` struct** — end-of-audio is signaled by the zero-length binary sentinel (4 zero bytes), not a JSON message; the `session.end` JSON exists only for engine-side mic mode (CLI `--mic`), not used by the app; all messages are JSON newline-delimited; binary frames (Phase 2) are NOT decoded via this protocol — they are raw 4-byte big-endian length + payload; `percent` in progress is `Double` (JSON `number`, always a float in range 0.0–1.0 — non-monotonic estimate for Path A; e.g. 0.42 means ~42% through token budget; engine never sends 0-100 scale)

- [x] [devops] Write `app/OpenVerb/Engine/EngineClient.swift`: `class EngineClient` (NOT actor — needs sync access for `sendAudioFrame` from audio thread) with internal `DispatchQueue(label: "io.openverb.engineclient", qos: .userInitiated)` for thread-safe fd access; `func connect(path: String) async throws` — POSIX `socket(AF_UNIX, SOCK_STREAM, 0)` + `connect()`; `func startSession(context: [String: String]) async throws` — encode SessionStart JSON + `\n`, write to fd; `func sendAudioFrame(_ data: Data)` — **synchronous** (called from audio thread callback without await): dispatches onto the internal serial queue with `queue.async { write... }` — **do NOT use `queue.sync`** here as the audio thread must not block; writes are fire-and-forget; if the kernel socket send buffer is full, `write()` returns `EAGAIN` — log and drop the frame (brief audio glitch is acceptable; engine-side ring buffer also protects against underrun); `func sendEndOfAudio()` — write 4 zero bytes (sentinel) on the internal queue; `func receiveMessage() async throws -> ServerMessage` — buffered read: `read()` up to 4096 bytes, append to `var accumulator: String` (grows dynamically — result JSON can exceed 4096 bytes for long dictations); scan for `\n`; on find: extract message up to `\n`, leave remainder in accumulator; decode JSON via EngineProtocol; `func sendPing() async throws` — send ping JSON, wait for pong response with 5s timeout; `func sendShutdown()` — send shutdown JSON on internal queue; `func disconnect()` — close fd; **Phase 2 concurrent read**: during binary streaming, start a `Task` that polls with POSIX `poll(fd, POLLIN, 100ms)` on a background thread — if POLLIN fires, read bytes into an accumulation buffer until `\n` is found (do NOT assume entire message fits in one `read()` call); once `\n` found → parse JSON; **first char check as sanity only**: if accumulated message does not start with `{` → treat as fatal protocol violation; on error message: set `phase2Error: ServerMessage?` (protected by `NSLock`) to abort the write loop; on warning (code `approaching_context_limit`): log via `os.Logger`, continue streaming; the write loop checks `phase2Error` before each `sendAudioFrame` and aborts if set; `func startPhase2Monitor()` / `func stopPhase2Monitor()`; **connection state**: track fd, expose `var isConnected: Bool`; on EPIPE/ECONNRESET: throw `ConnectionClosed` error; **retain cycle prevention**: `var onError: ((ServerMessage) -> Void)?` — callers MUST capture `[weak self]` in the closure assigned here; **error propagation**: when Phase 2 monitor detects an error, or when receiveMessage() gets an error, call `onError?(errorMessage)` so the UI can react (hide window, show error, transition state); all logging via `import os; private let logger = Logger(subsystem: "io.openverb.app", category: "EngineClient")`

- [x] [devops] Write `app/OpenVerb/Engine/EngineManager.swift`: `@MainActor class EngineManager: ObservableObject` — process lifecycle and crash recovery; `@Published var status: EngineStatus` enum `{ stopped, starting, running, error(String) }`; `func ensureRunning() async throws` — try connect + ping → on success, reuse; on fail → remove stale socket file at `Constants.DEFAULT_SOCKET_PATH` → spawn `openverb-engine --listen` as subprocess via `Process()` → poll socket up to 5s (100ms intervals) until ping succeeds; between stale socket removal and engine bind, another process could recreate the socket — acceptable race for single-session local tool; `func shutdown()` — send shutdown JSON via existing socket connection; if socket unresponsive within 1s, send SIGTERM via `Process.terminate()`; **do NOT close the socket before sending shutdown** — the shutdown message must go out on the live connection; `func disconnect()` — call after `shutdown()` returns or times out, then close fd; **crash recovery**: on dropped connection → log → restart with exponential backoff (1s, 2s, 4s delays); `var crashCounter: Int` — if engine crashes 3 times within 60s → stop auto-restarting, set status to `.error("Engine keeps crashing — try reducing --ctx-size or closing memory-intensive apps")`; **crash window**: record `firstCrashTime` on first crash; on each subsequent crash check `now - firstCrashTime < 60s`; if 3rd crash within window → stop; if any crash occurs after window expires → reset counter to 1 and `firstCrashTime = now` (sliding window from first crash); track `lastSuccessfulConnect` timestamp — if `now - lastSuccessfulConnect > 60s` on a new crash, the engine was stable, reset counter; **model check at launch**: `func checkModelExists() -> Bool` — check if any `.gguf` file exists in `Constants.DEFAULT_MODEL_DIR`; if missing → show `NSAlert` with message "Model not found. Run: `cp your-model.gguf ~/.openverb/models/`" and `NSApp.terminate(nil)` on dismiss; ModelDownloader.swift is NOT in MVP3 (deferred to MVP5); **engine binary resolution**: `var enginePath: String` — resolved at launch: (1) check `Bundle.main.bundlePath + "/Contents/MacOS/openverb-engine"` (bundled copy); (2) check `"/usr/local/bin/openverb-engine"` (installed); (3) if neither exists → `NSAlert` "openverb-engine binary not found" + exit; configurable via `--engine-path` arg; **sleep/wake handling**: register for `NSWorkspace.willSleepNotification` — on sleep: **STRICT ORDER**: (1) `audioSession.stop()` (discard in-progress audio), (2) `recordingWindow.hide()`, (3) `appState.transition(to: .idle)`, (4) `engineClient.sendShutdown()` — send shutdown while socket still open, (5) if engine unresponsive within 1s → `process.terminate()`, (6) `engineClient.disconnect()`; do NOT attempt inference — sleep notification budget is ~1-3s; on wake (`NSWorkspace.didWakeNotification`): restart engine process via `ensureRunning()`; model reloads lazily on next session (~4s cold load); show "Loading model..." in StatusBarItem during this window; total post-wake cold-start latency ~6s; logging via `import os; private let logger = Logger(subsystem: "io.openverb.app", category: "EngineManager")`

- [x] [tdd-guide] Write `app/OpenVerbTests/EngineProtocolTests.swift`: test ServerMessage decoding — `{"type":"session.ready"}\n` → `.ready`; `{"type":"pong"}\n` → `.pong`; `{"type":"progress","percent":0.425}\n` → `.progress(0.425)`; `{"type":"progress","percent":0.8}\n` → `.progress(0.8)` (float, range 0.0–1.0); `{"type":"result","text":"hello world","command":null}\n` → `.result(text: "hello world", command: nil)`; `{"type":"result","text":null,"command":{"action":"delete_last"}}\n` → `.result(text: nil, command: Command(action: "delete_last"))`; `{"type":"error","code":"session_limit","message":"engine busy"}\n` → `.error(...)`; `{"type":"warning","code":"approaching_context_limit","message":"..."}\n` → `.warning(...)`; test unknown type → throws/returns nil; test encoding of SessionStart, Ping, Shutdown → valid JSON with correct `type` field and trailing newline; **verify no SessionEnd struct exists** — end-of-audio is the binary sentinel, not a JSON message

- [x] [tdd-guide] Write `app/OpenVerbTests/EngineManagerTests.swift`: test crash counter — simulate 3 crashes within 60s → status becomes error with "Engine keeps crashing" message; test counter reset — crash, wait >60s (mock timer), crash again → counter at 1 not 2; test exponential backoff — first retry delay 1s, second 2s, third 4s; test model existence check — mock FileManager, model missing → returns false; note: actual process spawning and socket connection cannot be unit tested — those are integration test territory (human verification steps)

- [x] [architect] Write `app/OpenVerb/Input/HotkeyManager.swift`: `@MainActor class HotkeyManager` — manages ⌥Space global hotkey and Escape cancel gesture; **⌥Space**: register via `CGEvent.tapCreate(tap: .cgSessionEventTap, place: .headInsertEventTap, options: .listenOnly, eventsOfInterest: CGEventMask(1 << CGEventType.keyDown.rawValue), callback: ..., userInfo: ...)` — use `.listenOnly` (passive monitoring, requires **Input Monitoring** permission; `.defaultTap` would require Accessibility); **Input Monitoring permission is separate from Accessibility** — must be granted by the user in System Settings > Privacy & Security > Input Monitoring; at launch, check if `tapCreate` returns nil: first check if Input Monitoring is restricted (`IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)` returns `kIOHIDAccessTypeGranted`) — if not granted, show `NSAlert` explaining Input Monitoring is needed for the global hotkey with a button to open System Settings (`x-apple.systempreferences:com.apple.preference.security?Privacy_InputMonitoring`); if Input Monitoring IS granted but tapCreate still fails (key conflict), fall through to conflict resolution; callback checks for ⌥Space (virtualKey `0x31` = Space, flags `.maskAlternate`); **300ms debounce**: track `lastToggleTime`, ignore presses within 300ms window; debounce prevents state corruption from rapid double-taps; **Escape**: register **BOTH** monitors: (1) `NSEvent.addGlobalMonitorForEvents(matching: .keyDown)` — catches Escape when NSPanel is NOT key window (the common case with `nonactivatingPanel`); global monitor for modifier-free keys does NOT require Input Monitoring permission; (2) `NSEvent.addLocalMonitorForEvents(matching: .keyDown)` — catches Escape when NSPanel IS key window (less common but possible); both check `event.keyCode == 53`; **register both Escape monitors on IDLE→PREPARING transition** (user must be able to cancel during "Preparing..." cold-start window too); deregister both on transition to IDLE; do NOT leave monitors active during IDLE; during INFERRING, Escape monitors remain active but `onCancelRecording` is ignored (only `onAbortAndRestart` via ⌥Space is actionable in INFERRING); deduplicate: if both fire for the same event, guard with a `var escapeHandled = false` flag (reset per-event) so `onCancelRecording` is called only once; **hotkey conflict resolution**: if tapCreate returns nil and Input Monitoring IS granted → show `NSAlert` informing user that ⌥Space may be in use by another app; offer three fixed alternatives via `NSPopUpButton`: ⌥\` (backtick), ⌃Space, ⌥⇧Space; on selection → retry tapCreate with chosen key; do NOT use NSTextField for key capture (captures text characters, not key events); full custom shortcut UI is MVP5; expose callbacks: `var onToggleRecording: (() -> Void)?`, `var onCancelRecording: (() -> Void)?`, `var onAbortAndRestart: (() -> Void)?`; callback routing in OpenVerbApp: if state == .idle → `onToggleRecording`; if state == .recording → `onToggleRecording` (second press = confirm); if state == .inferring → `onAbortAndRestart`; if state == .preparing → ignore; if state == .error → ERROR→PREPARING via AppState

- [x] [devops] Write `app/OpenVerb/Input/AudioSession.swift`: `class AudioSession` — wraps AVAudioEngine for microphone capture; `func start(waveformCallback: @escaping (Data) -> Void) throws` — request microphone permission (`AVCaptureDevice.requestAccess(for: .audio)`); **if permission denied → throw error** (caller shows "Microphone permission needed" and transitions to ERROR); install tap on `audioEngine.inputNode` with buffer size 4096; configure `AVAudioConverter` with explicit output format: 16kHz sample rate, Int16 format (`AVAudioCommonFormat.pcmFormatInt16`), mono (1 channel); **do NOT assume hardware captures at 16kHz** — typical Mac hardware captures at 44.1kHz or 48kHz; tap callback fires on background audio thread; convert captured buffer to 16kHz/16-bit/mono via AVAudioConverter; deliver exactly 4096-byte chunks (128ms at 16kHz/16-bit/mono: 4096 / (16000 × 2) = 0.128s); **pre-buffer**: AudioSession starts capturing immediately (on ⌥Space), BEFORE session.start is sent; PCM chunks accumulate in an internal `var preBuffer: [Data] = []` (thread-safe via `os_unfair_lock`); the `waveformCallback` is called immediately for every chunk (waveform shows from first audio); but audio is NOT sent to engine yet — only buffered locally; `func flushAndSetSendCallback(_ callback: @escaping (Data) -> Void) -> [Data]` — **atomic operation under lock**: (1) copies and clears preBuffer, (2) sets sendCallback, (3) returns the copied chunks — caller sends them to engine *after* the lock is released; this prevents the race where a new chunk arrives between flush and setSendCallback and goes nowhere; the returned chunks are sent in order before live streaming begins; `func stop()` — under lock: clear preBuffer, clear sendCallback, set `_isCapturing = false`; then remove tap and stop AVAudioEngine; **thread safety**: `stop()` is called from main thread while tap callback runs on audio thread — all mutable state (`preBuffer`, `sendCallback`, `_isCapturing`) guarded by `os_unfair_lock`; `var isCapturing: Bool` — reads `_isCapturing` under lock; **mic permission check**: on each `start()` call, check `AVCaptureDevice.authorizationStatus(for: .audio)` — if `.denied` or `.restricted` → throw descriptive error so caller can show "Microphone permission needed" per spec failure mode table

- [x] [architect] Write `app/OpenVerb/Context/ContextBuilder.swift`: `struct ContextBuilder` — assembles context JSON for session.start; **MVP3 scope**: `app` + `clipboard` only — no Accessibility API (MVP4); `static func build(targetApp: NSRunningApplication?) async -> [String: String]` (async to avoid blocking main thread on clipboard read — large clipboard text adds latency to IDLE→PREPARING) — takes `targetApp` from `AppState.targetApp` (captured on IDLE→PREPARING); do NOT re-query `NSWorkspace.shared.frontmostApplication` — RecordingWindow may be foreground by then; `app`: `targetApp?.bundleIdentifier ?? targetApp?.localizedName ?? "unknown"`; `clipboard`: `NSPasteboard.general.string(forType: .string)` — truncate to **10,240 UTF-8 bytes** (measure encoded bytes, not character count — a single emoji or CJK character can be 4 bytes; use `String.utf8.prefix(10240)` then decode back to String); if clipboard is nil or clipboard string is empty after read, omit key from context dict (do not include empty string); other clipboard types (images, files) return nil from `string(forType: .string)` and are silently ignored; `language`: `Locale.current.language.languageCode?.identifier ?? "en"` (BCP-47); `window`: always `""` (empty string — Accessibility not available in MVP3; engine applies default style); `selection`: omit entirely (not available without Accessibility); **privacy**: clipboard content is read once, used only for the context JSON, never logged; output is `[String: String]` dict compatible with EngineProtocol.SessionStart

- [x] [tdd-guide] Write `app/OpenVerbTests/ContextBuilderTests.swift`: test basic assembly — provide mock NSRunningApplication with known bundleIdentifier → context dict contains `"app": "com.apple.Terminal"`; test bundleIdentifier nil → falls back to localizedName; test both nil → `"app": "unknown"`; test nil targetApp (passed nil) → `"app": "unknown"`; test clipboard truncation — 15KB string → truncated to 10KB in output; test empty clipboard → key omitted from dict; test language defaults to current locale; **note**: NSRunningApplication can be protocol-abstracted — define `protocol AppIdentifiable { var bundleIdentifier: String?; var localizedName: String? }`, extend NSRunningApplication to conform, and create test mock; NSPasteboard mocking via protocol `protocol PasteboardReadable { func string(forType: NSPasteboard.PasteboardType) -> String? }` with test impl

- [x] [architect] Write `app/OpenVerb/UI/RecordingWindow.swift`: `class RecordingWindow: NSPanel` — center-screen floating overlay; `styleMask: [.nonactivatingPanel, .fullSizeContentView, .borderless]` — does NOT steal focus from target app; `level: .floating`; `isMovableByWindowBackground = false`; `backgroundColor = .clear`; `hasShadow = false` (shadow on transparent panel renders around zero-alpha rect — instead, apply shadow to the SwiftUI content view's opaque background with `RoundedRectangle` + `.shadow(radius: 20)`; add extra padding around content so shadow is not clipped by window bounds); fixed size (~300×100pt including shadow padding, visual content ~300×80pt); **multi-display**: position on the screen containing the mouse cursor — `let screen = NSScreen.screens.first(where: { NSMouseInRect(NSEvent.mouseLocation, $0.frame, false) }) ?? NSScreen.main ?? NSScreen.screens[0]`; center horizontally on that screen, ~30% from top; **appearance**: set `appearance = nil` to inherit system appearance — the SwiftUI `RoundedRectangle` background should use `.background(.regularMaterial)` (vibrancy) which adapts to both light and dark mode automatically; **content**: SwiftUI `NSHostingView` with a `ZStack` containing `WaveformView` and `ProcessingView`, switching visibility based on `AppState.state`; **crossfade transition**: on RECORDING→INFERRING, WaveformView fades out over 150ms while ProcessingView fades in over 150ms; use `.transition(.opacity)` with `.animation(.easeInOut(duration: 0.15))`; do NOT use `.transition(.slide)` — causes layout jump; window height stays constant during transition (both views must have matching heights); **preparing subtitle**: when `AppState.preparingSubtitle` is non-nil, show centered text below the main content area; `func show()` — `orderFrontRegardless()`, recompute center position on current mouse screen; `func hide()` — `orderOut(nil)` — MUST be called BEFORE `targetApp.activate()` for text injection; if panel is key window when CGEvent fires, ⌘V goes to the panel not the target app

- [x] [devops] Write `app/OpenVerb/UI/WaveformView.swift`: `struct WaveformView: View` — displays real-time audio amplitude bars/waveform; `@ObservedObject var viewModel: WaveformViewModel` where `WaveformViewModel: ObservableObject` has `@Published var amplitudes: [CGFloat]` (ring buffer of recent RMS values, max 30 bars); `func updateAmplitude(_ data: Data)` — compute RMS from PCM Int16 samples in the data chunk; **threading**: this method is called from AudioSession's tap callback (background audio thread); `@Published` update MUST be dispatched to main thread: `DispatchQueue.main.async { self.amplitudes.append(rms); if self.amplitudes.count > maxBars { self.amplitudes.removeFirst() } }`; 128ms chunks arrive ~8 times/sec — this rate is well below SwiftUI's 60fps rendering budget, so no additional throttling is needed; simply dispatching to main is sufficient for smooth updates; render as horizontal bar chart with rounded caps; visual: bars with gradient (teal/blue), centered vertically; animation: `.animation(.linear(duration: 0.05), value: amplitudes)` for smooth bar transitions; **macOS display link note**: `CADisplayLink` is iOS-only; if future animation smoothing is needed beyond DispatchQueue, use `CVDisplayLink` or `Timer.scheduledTimer(withTimeInterval: 1/60.0, repeats: true)`; height must match ProcessingView height for seamless crossfade

- [x] [devops] Write `app/OpenVerb/UI/ProcessingView.swift`: `struct ProcessingView: View` — animated indicator during inference; `@ObservedObject var viewModel: ProcessingViewModel` where `ProcessingViewModel: ObservableObject` has `@Published var displayedPercent: Double = 0.0` and `@Published var showSpinner: Bool = false`; `func updateProgress(_ percent: Double)` — **monotonic clamp**: only update if `percent > displayedPercent` (hides Path A non-monotonic backtracking); `func reset()` — **RESET INVARIANT**: set `displayedPercent = 0.0` and `showSpinner = false` on every IDLE→PREPARING transition; if not reset, clamp variable retains 100 from prior session and ring never animates for next result; `func startSpinnerFallback()` — after 2s with no progress message, set `showSpinner = true`; visual: circular progress ring (determinate when percent available, indeterminate spinner when fallback); no numeric percentage label; height must match WaveformView for seamless crossfade

- [x] [devops] Write `app/OpenVerb/UI/StatusBarItem.swift`: `@MainActor class StatusBarItem` — `NSStatusBar.system.statusItem(withLength: .squareLength)`; icon: microphone glyph (`NSImage(systemSymbolName: "mic.fill")`); **states**: idle (normal icon), recording (red dot overlay or tinted icon), inferring (pulsing icon), error (yellow exclamation overlay); menu: "About OpenVerb", separator, "Quit OpenVerb" (`NSApp.terminate`); show engine status from EngineManager — "Loading model..." during cold start / post-wake, error messages from crash recovery; **no Preferences menu item** (MVP5)

- [x] [tdd-guide] Write `app/OpenVerbTests/ProcessingViewModelTests.swift`: test monotonic clamp — sequence [0.1, 0.2, 0.15, 0.3] → displayedPercent sequence [0.1, 0.2, 0.2, 0.3] (0.15 ignored because < 0.2); test reset — set displayedPercent to 0.8, call reset(), verify displayedPercent == 0.0; test spinner fallback — no progress for 2s → showSpinner == true; progress arrives after spinner → showSpinner == false, ring shows; test full cycle — reset → progress(0.25) → progress(0.5) → progress(1.0) → verify displayedPercent at each step

- [x] [architect] Write `app/OpenVerb/Output/TextInjector.swift`: `struct TextInjector` — clipboard simulation with **STRICT OPERATION ORDER** (order is critical for focus correctness): `static func inject(text: String, targetApp: NSRunningApplication, window: RecordingWindow) async` — (0) **guard**: `if targetApp.isTerminated { window.orderOut(nil); return }` — skip injection if target app was quit between recording and injection; (1) record `NSPasteboard.general.changeCount` and save current clipboard string; (2) write `text` to `NSPasteboard.general` via `setString(_:forType: .string)`; (3) `window.orderOut(nil)` — hide panel, release key window status; (4) `targetApp.activate(options: [])` — bring target app to foreground; focus transfer is async (Mach message to Window Server); (5) **50ms delay** — `try await Task.sleep(for: .milliseconds(50))` — minimum that reliably covers focus transfer under load; 0ms works in benchmarks but fails intermittently under pressure; (6) simulate ⌘V via CGEvent: create `CGEvent(keyboardEventSource: nil, virtualKey: 0x09, keyDown: true/false)` with `.maskCommand` flag; `event.post(tap: .cghidEventTap)` — posts to HID event stream, delivered to now-active target app; (7) **300ms delay** via `try await Task.sleep(for: .milliseconds(300))` — wait for target app to read clipboard before restoring; note: slow apps (Electron, Word) may take longer; 300ms is best-effort — if clipboard is restored before the app reads it, the paste is lost; this is a known limitation of clipboard simulation (use CGEvent per-char fallback in MVP5); (8) restore original clipboard ONLY IF `NSPasteboard.general.changeCount` still equals saved value — if another process wrote to clipboard during the 300ms window, skip restore to avoid overwriting their data; **Accessibility permission note**: CGEvent posting requires the app to be in System Settings > Accessibility list (`AXIsProcessTrusted()`) — checked at launch in OpenVerbApp; **fallback**: CGEvent keystroke per character for fields where ⌘V is blocked (not implemented in MVP3 — deferred to MVP5)

- [x] [devops] Write `app/OpenVerb/Output/CommandExecutor.swift`: `struct CommandExecutor` — executes engine voice commands via CGEvent; `static func execute(command: Command, targetApp: NSRunningApplication, window: RecordingWindow) async` — match `command.action`: `"delete_last"` → ⌘Z (undo last paste — OpenVerb injected via ⌘V, undo reverses it); `"undo"` → ⌘Z (same keystroke as delete_last, semantic distinction only); `"insert_newline"` → Return (`kVK_Return = 0x24`, no modifiers); `"insert_newparagraph"` → Return + Return (two sequential CGEvents, 50ms apart via `Task.sleep`); unknown action → log warning via `os.Logger`, no-op; **MUST hide RecordingWindow before firing** — same focus sequence as TextInjector: `window.orderOut(nil)` → `targetApp.activate()` → 50ms delay → CGEvent; all CGEvents target the frontmost app captured at ⌥Space time (from AppState.targetApp); punctuation ("period", "comma") is NOT a command — Gemma outputs `.` or `,` directly as text, handled by TextInjector

- [x] [tdd-guide] Write `app/OpenVerbTests/CommandExecutorTests.swift`: test command action mapping — `"delete_last"` recognized as valid, `"undo"` valid, `"insert_newline"` valid, `"insert_newparagraph"` valid; `"unknown_command"` → no-op; `""` (empty action string) → no-op; test that `Command(action: "delete_last")` and `Command(action: "undo")` both map to ⌘Z; **note**: actual CGEvent posting cannot be unit tested — verified in human testing steps; these tests validate the parsing/mapping layer only; define `protocol EventPoster { func postKeyDown(virtualKey: CGKeyCode, flags: CGEventFlags) }` for testability

- [x] [devops] Write `app/OpenVerb/App/OpenVerbApp.swift`: `@main struct OpenVerbApp: App` with `@NSApplicationDelegateAdaptor(AppDelegate.self) var delegate`; `AppDelegate: NSObject, NSApplicationDelegate` owns: `appState: AppState`, `engineManager: EngineManager`, `engineClient: EngineClient` (class, not actor), `hotkeyManager: HotkeyManager`, `audioSession: AudioSession`, `recordingWindow: RecordingWindow`, `waveformVM: WaveformViewModel`, `processingVM: ProcessingViewModel`, `statusBar: StatusBarItem`; logging: `import os; private let logger = Logger(subsystem: "io.openverb.app", category: "App")`; **retain cycles**: all closures assigned to engineClient.onError, hotkeyManager callbacks, etc. MUST capture `[weak self]`

**applicationDidFinishLaunching**:
- [x] Check `AXIsProcessTrusted()` — if not, show NSAlert explaining Accessibility is needed for text injection + open System Settings; do NOT exit — text injection will fail silently until permission is granted
- [x] `engineManager.checkModelExists()` — if missing, NSAlert + exit
- [x] `Task { try await engineManager.ensureRunning() }` — start/connect to engine in background
- [x] `hotkeyManager.register()` — registers ⌥Space tap; on permission failure, shows Input Monitoring alert; on conflict, shows alternative picker
- [x] `engineClient.onError = { [weak self] msg in self?.handleEngineError(msg) }` — bridge errors to AppState
- [x] Create `statusBar`
- [x] Register `NSWorkspace.willSleepNotification` → `handleSleep()` and `NSWorkspace.didWakeNotification` → `handleWake()`

**applicationWillTerminate**: `engineManager.shutdown()` — sends session.shutdown and waits up to 1s; if engine doesn't respond, sends SIGTERM; prevents orphaned engine processes

**⌥Space toggle flow** (hotkeyManager.onToggleRecording):

*State IDLE or ERROR*: start session flow:
- [x] Check mic permission: `AVCaptureDevice.authorizationStatus(for: .audio)` — if `.denied`/`.restricted` → show "Microphone permission needed" → return
- [x] `appState.transition(to: .preparing)` → `processingVM.reset()`
- [x] `audioSession.start(waveformCallback: { [weak self] chunk in self?.waveformVM.updateAmplitude(chunk) })` — starts pre-buffering immediately; if throws → `audioSession` is not started → `appState.transition(to: .error("Microphone unavailable"))` → return
- [x] `NSSound(named: "Tink")?.play()` — recording start sound
- [x] `recordingWindow.show()`
- [x] Start 500ms timer: if still in `.preparing` → `appState.preparingSubtitle = "Preparing..."`
- [x] `Task { [weak self] in` — engine connect + session start:
   - `try await self?.engineManager.ensureRunning()` — reconnects if engine crashed since launch
   - `let context = await ContextBuilder.build(targetApp: self?.appState.targetApp)`
   - `try await self?.engineClient.startSession(context: context)`
   - On `.ready` received → `appState.transition(to: .recording)` → **flush pre-buffer atomically**: `let buffered = audioSession.flushAndSetSendCallback { [weak self] chunk in self?.engineClient.sendAudioFrame(chunk) }` → `for chunk in buffered { engineClient.sendAudioFrame(chunk) }` → `engineClient.startPhase2Monitor()`
   - On error → `audioSession.stop()` → `appState.transition(to: .error("Connection failed"))` (stop audioSession if startSession throws)
   - `}`

*State RECORDING* (second press = confirm):
- [x] `audioSession.stop()`
- [x] `engineClient.stopPhase2Monitor()`
- [x] `engineClient.sendEndOfAudio()`
- [x] `appState.transition(to: .inferring)`
- [x] `NSSound(named: "Pop")?.play()`
- [x] Start 2s timer → `processingVM.startSpinnerFallback()` if no progress by then
- [x] Drain progress/result in background Task (see result/error handling below)

*State INFERRING* (abort+restart):
- [x] `engineClient.disconnect()` — closing the socket causes the engine's session to detect `ConnectionClosed` in its inference poll loop; it sets `stop_requested_=true`, joins the inference thread (blocking until the model hits its abort check), then the session is **DESTROYED** (not returned to IDLE). The IpcServer re-enters `accept()` on the next poll cycle. The Swift client's 1.5s wait + `ensureRunning()`'s 5s polling window is sufficient to cover the full abort window.
- [x] `appState.transition(to: .preparing)` → `appState.preparingSubtitle = "Reconnecting..."`
- [x] Wait 1.5s for engine to finish aborting: `try await Task.sleep(for: .milliseconds(1500))`
- [x] Then same flow as IDLE start (step 3 onward above) — `targetApp` is preserved by INFERRING→PREPARING transition

*State PREPARING*: ignore

**Escape cancel** (hotkeyManager.onCancelRecording, fires in PREPARING or RECORDING; ignored in INFERRING):
- [x] `audioSession.stop()`
- [x] `engineClient.disconnect()` — engine detects EOF, returns to IDLE
- [x] `recordingWindow.hide()`
- [x] `appState.transition(to: .idle)`

**Result handling** (on `.result(text, command)` received in background Task):
- [x] Command takes priority: if `command != nil && !command.action.isEmpty` → `await CommandExecutor.execute(command!, appState.targetApp!, recordingWindow)` (window hidden inside executor)
- [x] Else if `text != nil && !text.isEmpty` → `await TextInjector.inject(text!, appState.targetApp!, recordingWindow)` (window hidden inside injector)
- [x] Else (empty text, nil command — VAD-filtered-to-zero or ultra-short audio) → `recordingWindow.hide()` silently, no error, no sound
- [x] `appState.transition(to: .idle)`

**Error handling** (on `.error(code, message)` or caught exception):
- [x] `recordingWindow.hide()`
- [x] `audioSession.stop()` (guard — may already be stopped)
- [x] Map code to user message: `"inference_failed"` → "Could not process — try again"; `"timeout"` → "Inference timed out"; `"duration_exceeded"` → "Recording too long"; model load error → "Model error — check ~/.openverb/models/"; default → engine message verbatim
- [x] `appState.transition(to: .error(userMessage))`; StatusBarItem shows error briefly
- [x] `NSSound(named: "Basso")?.play()` — error sound
- [x] Auto-clear: 5s timer → `appState.transition(to: .idle)` unless already transitioned

**Progress handling**: on `.progress(percent)` → `processingVM.updateProgress(percent)`

**Warning handling**: on `.warning(code, message)` → `logger.warning(...)`, continue (session not ended)

**Sleep handling** (`handleSleep()`):
- [x] `audioSession.stop()` — discard in-progress audio
- [x] `recordingWindow.hide()`
- [x] `appState.transition(to: .idle)`
- [x] `engineManager.shutdown()` — sends shutdown via live socket, then disconnects; falls back to SIGTERM after 1s (see step 6)

**Wake handling** (`handleWake()`):
- [x] `statusBar.showStatus("Loading model...")` → `Task { try await engineManager.ensureRunning(); statusBar.clearStatus() }`

- [x] [devops] Write `app/OpenVerb/Info.plist`: `NSMicrophoneUsageDescription = "OpenVerb needs microphone access to record your voice for transcription"`; `LSUIElement = true` (no dock icon — menu bar app only); `CFBundleIdentifier = io.openverb.app`; `CFBundleName = OpenVerb`; `LSMinimumSystemVersion = 13.0`; add `OpenVerb.entitlements` with `com.apple.security.device.audio-input = true`; **three permissions required at runtime** (none via entitlement — all require user approval in System Settings):
- **Microphone**: prompted by AVAudioEngine on first `start()` — entitlement `audio-input` enables the prompt; usage string in Info.plist provides the message
- **Input Monitoring**: required for `CGEvent.tapCreate(.listenOnly)` global hotkey; NOT promptable via API on macOS 13+ (system handles prompt on first tap attempt); if denied, show NSAlert with link to `x-apple.systempreferences:com.apple.preference.security?Privacy_InputMonitoring`
- **Accessibility**: required for `CGEvent.post()` text injection; check `AXIsProcessTrusted()` at launch; preferred approach: call `AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt.takeUnretainedValue() as String: true as AnyObject] as CFDictionary)` which shows the system-native prompt on all macOS versions; if user dismisses without granting, show NSAlert with link to `x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility` — **note**: this URL scheme works on macOS 13 (Ventura) and later where System Preferences was renamed to System Settings; on macOS 12 (Monterey) use `com.apple.preferences.security.remoteaccesspref` instead, or open `"/System/Library/PreferencePanes/Security.prefPane"` as a fallback

- [x] [devops] Configure Xcode project build settings: add all `.swift` source files to the `OpenVerb` target; add test files to `OpenVerbTests` target; set `SWIFT_VERSION = 5.9`; set `MACOSX_DEPLOYMENT_TARGET = 13.0`; link frameworks: `AVFoundation.framework`, `Carbon.framework`, `ApplicationServices.framework`; set `CODE_SIGN_IDENTITY = "Apple Development"` (or `-` for ad-hoc if no developer account); **add Copy Files build phase to bundle the engine binary**: Build Phases → Copy Files → Destination: "Executables"; File: `openverb-engine` (the compiled C++ binary from `engine/build/openverb-engine`); this copies it to `OpenVerb.app/Contents/MacOS/openverb-engine` so `EngineManager` finds it at `Bundle.main.bundlePath + "/Contents/MacOS/openverb-engine"`; in Xcode you must add a pre-build script or dependency that builds the engine first: "Build Phases → New Run Script Phase" (before Compile Sources): `cd "${SRCROOT}/../engine" && cmake -B build -DCMAKE_BUILD_TYPE=Release -DGGML_METAL=ON && cmake --build build -j$(sysctl -n hw.ncpu)`; create scheme `OpenVerb` with build + test actions; verify `xcodebuild -scheme OpenVerb -configuration Debug build` succeeds with zero errors; verify `xcodebuild -scheme OpenVerb test` runs all unit tests

- [ ] [general] HUMAN:
Build macOS app: `cd app && xcodebuild -scheme OpenVerb -configuration Release build`

- [ ] [general] HUMAN:
Launch OpenVerb.app — verify: status bar icon appears (mic glyph), no dock icon (LSUIElement), no main window

- [ ] [general] HUMAN:
Remove model from `~/.openverb/models/` → relaunch app → verify NSAlert "Model not found. Download with: ./scripts/download-model.sh" appears and app exits on dismiss

- [ ] [general] HUMAN:
Grant Microphone + Input Monitoring + Accessibility permissions → press ⌥Space → verify recording window appears center-screen as floating panel, target app stays focused behind it

- [ ] [general] HUMAN:
Speak into microphone — verify waveform animates in real-time in the recording window, responsive to voice volume

- [ ] [general] HUMAN:
Press ⌥Space again → verify: waveform crossfades to processing indicator (150ms), engine processes audio, result text appears at cursor position in the target app (e.g., TextEdit), recording window auto-hides

- [ ] [general] HUMAN:
Start recording → press Escape → verify: recording window hides immediately, no result produced, no error shown, engine resets to IDLE (check engine --verbose logs)

- [ ] [general] HUMAN:
Start recording → say "delete that" → press ⌥Space → verify: ⌘Z is fired in target app (undo last action), not text "delete that" injected; repeat with "new line" → verify Return key fired

- [ ] [general] HUMAN:
With engine running, `kill -9 $(pgrep openverb-engine)` → verify app detects disconnect, auto-restarts engine (check stderr logs for backoff), new ⌥Space session works after restart

- [ ] [general] HUMAN:
Start recording → press ⌥Space (confirm) → while processing indicator shows, press ⌥Space again → verify: inference aborts, "Reconnecting..." subtitle shows briefly, new recording session starts automatically
